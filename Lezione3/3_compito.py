# 1. [FACILE] Chiedere all'utente di inserire numeri finché non viene inserito un numero negativo.
# Salvare i numeri inseriti dall'utente in una lista e, al termine degli inserimenti, stampare tutti i valori inseriti.

# 2. [FACILE] Chiedere all'utente di inserire una stringa. Effettuare una serie di controlli sulla stringa inserita.
# Stampare se la stringa inserita:
# - Inizia con la lettera "k"
# - Finisce con la desinenza "are"
# - Contiene la stringa "fe"
# - Contiene almeno una cifra numerica

# 3. [INTERMEDIO] Chiedere all'utente di inserire stringhe finché viene inserita la stringa "stop".
# Togliere da tutte le stringhe inserite la sottostringa "ko" e ristamparle.

# 4. [DIFFICILE] Data la seguente lista di numeri interi, ordinarla (in ordine crescente o decrescente) senza usare la funzione "sort()"
lista = [55, -20, 12, 1, 0, 99, -2, 5]